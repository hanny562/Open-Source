var keys = document.querySelectorAll('#calculator span');
var operators = ['+', '-', 'x', '/', '%', 'Xy'];
var decimalAdded = false;

function calculate(select) {

    var symbol = {
        sum: '+',
        minus: '-',
        div: '/',
        mul: '*',
        mod: '%',
        power: '^'
    };

   
}